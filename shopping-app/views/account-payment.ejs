<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title><%= title %></title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

  <link rel="stylesheet" href="/css/account-common.css">
  <link rel="stylesheet" href="/css/checkout.css" />
  <link rel="stylesheet" href="/css/account.css" />

</head>
<body>
  <%- include('components/navBar') %>

  <div class="account-page">
    <div class="breadcrumb">Home / My Account</div>

    <div class="account-layout">
      <aside class="account-sidebar">
        <h3>Manage My Account</h3>
        <ul>
          <li class="<%= activePage === 'profile' ? 'active' : '' %>"><a href="/account">My Profile</a></li>
          <li class="<%= activePage === 'address' ? 'active' : '' %>"><a href="/account/address">Address Book</a></li>
          <li class="<%= activePage === 'payment' ? 'active' : '' %>"><a href="/account/payment">My Payment Options</a></li>
        </ul>
      </aside>

      <main class="account-card">
        <div class="account-topbar">
          <h2 class="account-title">My Payment Options</h2>
          <div class="welcome">Welcome! <b><%= user.firstName %></b></div>
        </div>

        <div class="form-grid">
          <div class="form-row">
            <label>Payment Type</label>
            <select>
              <option selected>Card</option>
              <option>PayPal</option>
              <option>Afterpay (Mock)</option>
            </select>
          </div>

          <div class="form-row">
            <label>Name on Card</label>
            <input value="<%= payment.name %>" />
          </div>

          <div class="form-row">
            <label>Card Number (Mock)</label>
            <input value="**** **** **** <%= payment.last4 %>" />
          </div>

          <div class="form-row">
            <label>Expiry (MM/YY)</label>
            <input value="<%= payment.expiry %>" />
          </div>
        </div>

        <div class="actions">
          <button class="btn btn-link">Cancel</button>
          <button class="btn btn-primary">Save Payment</button>
        </div>
      </main>
    </div>
  </div>
</body>
</html>
